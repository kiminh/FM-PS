#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.14.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
if sys.version_info[0] > 2:
    from urllib.parse import urlparse
else:
    from urlparse import urlparse
from thrift.transport import TTransport, TSocket, TSSLSocket, THttpClient
from thrift.protocol.TBinaryProtocol import TBinaryProtocol

from task import MLtask
from task.ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
    print('')
    print('Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] [-s[sl]] [-novalidate] [-ca_certs certs] [-keyfile keyfile] [-certfile certfile] function [arg1 [arg2...]]')
    print('')
    print('Functions:')
    print('  bool submit(string dist_info, string network_struct)')
    print('  string query()')
    print('  i32 worker_regist_to_master()')
    print('  string worker_ask_for_task()')
    print('  void worker_submit_kth_result(string kth_result)')
    print('  void server_regist_to_master(string server_info)')
    print('  string server_ask_for_task()')
    print('')
    sys.exit(0)

pp = pprint.PrettyPrinter(indent=2)
host = 'localhost'
port = 9090
uri = ''
framed = False
ssl = False
validate = True
ca_certs = None
keyfile = None
certfile = None
http = False
argi = 1

if sys.argv[argi] == '-h':
    parts = sys.argv[argi + 1].split(':')
    host = parts[0]
    if len(parts) > 1:
        port = int(parts[1])
    argi += 2

if sys.argv[argi] == '-u':
    url = urlparse(sys.argv[argi + 1])
    parts = url[1].split(':')
    host = parts[0]
    if len(parts) > 1:
        port = int(parts[1])
    else:
        port = 80
    uri = url[2]
    if url[4]:
        uri += '?%s' % url[4]
    http = True
    argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
    framed = True
    argi += 1

if sys.argv[argi] == '-s' or sys.argv[argi] == '-ssl':
    ssl = True
    argi += 1

if sys.argv[argi] == '-novalidate':
    validate = False
    argi += 1

if sys.argv[argi] == '-ca_certs':
    ca_certs = sys.argv[argi+1]
    argi += 2

if sys.argv[argi] == '-keyfile':
    keyfile = sys.argv[argi+1]
    argi += 2

if sys.argv[argi] == '-certfile':
    certfile = sys.argv[argi+1]
    argi += 2

cmd = sys.argv[argi]
args = sys.argv[argi + 1:]

if http:
    transport = THttpClient.THttpClient(host, port, uri)
else:
    if ssl:
        socket = TSSLSocket.TSSLSocket(host, port, validate=validate, ca_certs=ca_certs, keyfile=keyfile, certfile=certfile)
    else:
        socket = TSocket.TSocket(host, port)
    if framed:
        transport = TTransport.TFramedTransport(socket)
    else:
        transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol(transport)
client = MLtask.Client(protocol)
transport.open()

if cmd == 'submit':
    if len(args) != 2:
        print('submit requires 2 args')
        sys.exit(1)
    pp.pprint(client.submit(args[0], args[1],))

elif cmd == 'query':
    if len(args) != 0:
        print('query requires 0 args')
        sys.exit(1)
    pp.pprint(client.query())

elif cmd == 'worker_regist_to_master':
    if len(args) != 0:
        print('worker_regist_to_master requires 0 args')
        sys.exit(1)
    pp.pprint(client.worker_regist_to_master())

elif cmd == 'worker_ask_for_task':
    if len(args) != 0:
        print('worker_ask_for_task requires 0 args')
        sys.exit(1)
    pp.pprint(client.worker_ask_for_task())

elif cmd == 'worker_submit_kth_result':
    if len(args) != 1:
        print('worker_submit_kth_result requires 1 args')
        sys.exit(1)
    pp.pprint(client.worker_submit_kth_result(args[0],))

elif cmd == 'server_regist_to_master':
    if len(args) != 1:
        print('server_regist_to_master requires 1 args')
        sys.exit(1)
    pp.pprint(client.server_regist_to_master(args[0],))

elif cmd == 'server_ask_for_task':
    if len(args) != 0:
        print('server_ask_for_task requires 0 args')
        sys.exit(1)
    pp.pprint(client.server_ask_for_task())

else:
    print('Unrecognized method %s' % cmd)
    sys.exit(1)

transport.close()
